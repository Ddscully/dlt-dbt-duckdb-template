name: warehouse
type: duckdb
options:
  # Relative to this directory: the repo's data/warehouse.duckdb, which has to be
  # built first. EVIDENCE_SOURCE__warehouse__filename overrides it, but is joined
  # onto this directory too, so an absolute path is silently relocated.
  filename: ../../../data/warehouse.duckdb
